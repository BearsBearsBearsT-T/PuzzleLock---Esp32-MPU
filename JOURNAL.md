Making the scamatic:

I decided to use an ESP32 Devkit instead of the ESP32 module so soldering would be a little easier.
I might later switch this out for pins so I can solder an ESP32 bored on instead. 
